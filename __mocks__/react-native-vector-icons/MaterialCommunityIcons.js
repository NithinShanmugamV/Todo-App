import React from 'react';

const MaterialCommunityIcons = (props) => {
  // You can customize the mock icon as needed
  return <MockedIcon {...props} />;
};

// You might need to replace 'MockedIcon' with a suitable mock component
const MockedIcon = (props) => {
  return <></>; // Empty component
};

export default MaterialCommunityIcons;
